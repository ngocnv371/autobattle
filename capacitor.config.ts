import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.ngocnv371.battle',
  appName: 'Battle',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
